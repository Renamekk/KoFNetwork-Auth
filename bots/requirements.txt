# Список для установки без uv — им пользуется Dockerfile.
# Версии закреплены по минимуму: боты не переживут смены мажорной версии
# aiogram или discord.py, а патчи забирать полезно.
#
# Клиента Redis здесь больше нет намеренно. Боты получали мастер-пароль
# хранилища ради чтения одного канала — вместе с правом переписывать состояния
# входа и публиковать любые события от имени системы. Сообщения приходят через
# /api/bot/events, и ключа /api/bot достаточно.
httpx>=0.27,<1.0
aiogram>=3.13,<4.0
discord.py>=2.4,<3.0
