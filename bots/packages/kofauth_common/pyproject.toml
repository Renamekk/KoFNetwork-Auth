[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kofauth-common"
version = "1.0.0"
description = "Общий код ботов KoF Network"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[tool.hatch.build.targets.wheel]
packages = ["kofauth_common"]
